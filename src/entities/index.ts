/**
 * Entity factory exports
 *
 * These factories encapsulate entity creation with all required components
 * and sensible defaults, making entity setup more maintainable.
 */

export { createPlayer } from "./createPlayer";
export { createNPC, type NPCOptions } from "./createNPC";
